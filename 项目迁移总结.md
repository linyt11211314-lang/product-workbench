# 拾光柠工作台 · AI Listing 工坊 — 项目迁移总结

> 生成日期：2026-08-10 ｜ 版本：1.0.0 ｜ 技术栈：Node.js (Express) + 原生前端 (ES Modules)

---

## 一、项目简介

**拾光柠工作台**是一个「亚马逊产品开发内容生成中心」，核心模块为 **AI Listing 工坊**：产品信息输入 → AI 分析 → 生成 Amazon Listing → 人工编辑优化 → 保存项目。附带**选品库**（产品素材管理 + 多站点利润测算）与**设置**（AI 服务配置 / 主题 / 默认参数）。

纯前端 + 轻量后端代理架构，数据全部存储在浏览器 localStorage（无需数据库）。

---

## 二、功能清单

| 模块 | 功能 |
|---|---|
| **首页** | 固定高度指标卡（项目数/今日生成/选品数/AI 调用）、快捷入口、最近项目 |
| **选品库** | 列表视图（图片/名称/采购成本/定价/规格/利润总览/选品时间）、批量删除、Excel 导出/导入（同一模板）、图片上传（单击选中/双击上传/Ctrl+V 粘贴） |
| **利润测算** | 多站点联动（一份基础数据 → 每个勾选站点独立报价）、汇率随站点、体积重头程公式（体积重与实重取大 × 海运单价 ÷ 汇率，除数可自定义）、FBA 手动填写、VAT/佣金/广告/仓储/退货固定百分比、每站参数可独立修改 |
| **AI Listing 工坊** | 创建表单（必填：图片/名称/类目/站点/核心卖点；可选：竞品链接/关键词/品牌/禁止词/规格）、DeepSeek 生成 5 分区（标题/五点/描述/Search Terms/图片文案）+ 可选竞品分析、每分区独立编辑/复制/重新生成、项目持久化 |
| **设置页** | DeepSeek API Key（真实可用：保存/测试连接）、主题外观（白/黑底 × 5 主题色）、利润测算默认参数（体积重除数、海运单价、12 站点 × 7 参数，支持勾选批量设置）、自动保存草稿 |

---

## 三、系统架构

```
浏览器页面 (public/)
  ├─ pages/    home / library / listing / settings
  ├─ services/ aiProvider（AI 抽象层）· listingService（Prompt 编排）· pricing（利润测算）· productTransfer（Excel）
  ├─ store/    settingsStore · productStore · projectStore · statsStore  （localStorage 持久化）
  └─ ui/       icons · toast · modal · fields

后端 (server.js, Express)
  ├─ 静态托管 public/
  ├─ POST /api/ai/generate        → DeepSeek 代理（Key 来自浏览器设置，随请求头传递）
  ├─ POST /api/ai/test            → DeepSeek 连通性测试
  ├─ POST /api/listing/generate   → 独立生成接口（Key 读环境变量 DEEPSEEK_API_KEY，固定模板）
  └─ GET  /api/health             → 健康检查

AI 链路（页面不直连 DeepSeek）：
Listing 页面 → Listing Service（Prompt 组装/解析）→ AI Provider → 后端代理 → DeepSeek API
```

---

## 四、目录结构

```
shiguangning-workbench/
├── server.js                  # 后端服务（Express，单文件）
├── package.json               # 依赖：express + xlsx；npm start 启动
├── package-lock.json
├── README.md                  # 原始说明文档
├── 项目迁移总结.md             # 本文档
├── preview.html               # 本地预览汇总页（含截图）
├── preview-assets/            # 功能截图（9 张）
└── public/
    ├── index.html             # 应用入口（单页）
    ├── api-tester.html        # 独立接口测试台（POST /api/listing/generate）
    ├── css/styles.css         # 全局样式（主题系统：明暗 × 5 色）
    ├── vendor/xlsx.full.min.js# SheetJS（本地托管，离线可用）
    └── js/
        ├── app.js             # 入口：导航路由 + 初始化
        ├── config.js          # 站点配置（12 站点汇率/货币/FBA 默认）、类目建议、DeepSeek 模型
        ├── utils.js
        ├── pages/             # home / library / listing / settings
        ├── services/          # aiProvider / listingService / pricing / productTransfer
        ├── store/             # settingsStore / productStore / projectStore / statsStore
        └── ui/                # icons / toast / modal / fields
```

---

## 五、运行方式

### 环境要求
- **Node.js ≥ 18**（后端使用内置 `fetch` 调用 DeepSeek）
- 无需数据库、无需 Redis，纯静态 + 轻代理

### 启动步骤
```bash
# 1. 安装依赖（迁移包已含 node_modules 时可跳过）
npm install

# 2. 启动（默认端口 3000）
npm start
# 或指定端口：PORT=8080 npm start

# 3. 浏览器访问
http://localhost:3000
```

### 环境变量
| 变量 | 用途 |
|---|---|
| `PORT` | 服务端口，默认 3000 |
| `DEEPSEEK_API_KEY` | **可选**。仅用于独立接口 `POST /api/listing/generate`（Key 从环境变量读取） |

> ⚠️ 主工作台（AI Listing 工坊）的 DeepSeek Key 在**浏览器设置页**配置（存 localStorage），不需要环境变量。

---

## 六、API 接口

### 1. 工作台代理（页面使用）
- `POST /api/ai/generate` — DeepSeek 生成代理。请求体含 `{ apiKey, model, messages }`，Key 由前端设置页提供
- `POST /api/ai/test` — Key 连通性测试
- `GET /api/health` — 健康检查，返回 `{"ok":true}`

### 2. 独立生成接口（外部程序集成）
- `POST /api/listing/generate` — 自动生成亚马逊 Listing
  - 参数：`product_name`（必填）、`material`、`key_points`（逗号分隔）
  - 返回：`{ code: 0, data: { title, bullet_points, description, search_terms }, message: "success" }`
  - 模型：`deepseek-v4-flash`（如模型不存在，改 `server.js` 中 `MODEL` 常量为 `deepseek-chat`）

```bash
curl -X POST http://localhost:3000/api/listing/generate \
  -H "Content-Type: application/json" \
  -d '{"product_name":"Mini Portable Blender","material":"Tritan","key_points":"USB-C charging, 350ml, quiet motor"}'
```

---

## 七、数据存储说明（重要）

**所有业务数据存浏览器 localStorage，无服务端存储。** 迁移/换机后数据不跟随，需注意：

| 存储键 | 内容 |
|---|---|
| `sgn.products` | 选品库产品（含多站点 quotes、货源、报价） |
| `sgn.listingProjects` | AI Listing 项目（标题/五点/描述/关键词/图片文案/竞品分析） |
| `sgn.settings` | 设置（API Key / 模型 / 主题 / 利润测算默认参数 quoteDefaults） |
| `sgn.stats` | 统计（AI 调用次数、生成数） |

> 迁移数据方法：在旧浏览器控制台执行 `Object.keys(localStorage).forEach(k=>console.log(k+'='+localStorage.getItem(k)))` 导出，或使用各浏览器自带的数据导出功能；在新环境控制台写回。

---

## 八、关键配置入口

| 配置 | 位置 | 说明 |
|---|---|---|
| DeepSeek API Key | 设置 → DeepSeek API 配置 | 保存后真实调用；测试连接按钮验证 |
| 主题 | 设置 → 主题外观 | 浅色/深色 × 荧光黄/蜜桃粉/玫红/雾蓝/果绿 |
| 利润测算默认参数 | 设置 → 利润测算默认参数 | 体积重除数（默认 6000）、海运单价、12 站点 × 7 参数（汇率/目标利润/广告/佣金/VAT/仓储/退货），支持勾选批量设置 |
| 12 站点内置配置 | `public/js/config.js` | `AMAZON_SITES`（汇率/货币符号）、`PER_SITE_RATES`（VAT/仓储/退货默认） |

---

## 九、迁移注意事项

1. **node_modules**：迁移包已包含（express + xlsx，纯 JS 跨平台）；若环境不一致或需清理，删除后 `npm install` 即可
2. **端口**：默认 3000，可用 `PORT` 覆盖
3. **localStorage 数据**：不随代码迁移，用户数据需单独导出/导入（见第七节）
4. **DeepSeek Key**：主工作台 Key 在浏览器设置页配置；独立接口 Key 走环境变量 `DEEPSEEK_API_KEY`
5. **Excel 模板**：选品库「导出 Excel」生成模板与「导入 Excel」同构，模板内「重量(g)」列语义为克（页面输入 kg 自动换算）
6. **HTTPS 代理**：若部署在反代/HTTPS 下，无需额外配置（纯静态 + 相对路径 API）

---

## 十、近期功能演进记录

- 多站点利润测算：一份基础数据 → 每站点独立报价，汇率随站点
- 体积重头程公式：计费重 = max(实重, 长×宽×高÷除数) × 海运单价 ÷ 汇率
- 利润测算默认参数可配置（设置页），支持勾选批量设置
- 仓储费改为固定百分比（与退货/佣金/广告一致）
- FBA 配送费改为手动填写（不内置费率表）
- Amazon 2026 新规：75 字符标题 + Item Highlights 动态 Prompt（例外站点 EG/SA/TR/AE 用 200 字符旧规）
- Excel 导出/导入选品库（同模板，报价自动重算）
- 主题系统（明暗 × 5 色）+ 全局字号/荧光黄配色
